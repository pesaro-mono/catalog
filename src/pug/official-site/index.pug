extends /template/_default

block content
  .Index
    - var length = 26;
    - for(var i = 1; i <= length; i++)
      .item(class=`item--${i}`)
        img.img(src=`assets/images/${i}.jpg`)
        if [6, 8, 15].includes(i)
          video.mp4(class=`mp4--${i}` src=`assets/mp4/${i}.mp4` autoplay loop muted playsinline)
